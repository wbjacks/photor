public abstract class BaseController {

    public registerController() {
        // TODO: (wbjacks) Use reflection to call all public methods
        for (Method method : self.getClass().getMethods()) {
            

        }
    }
}
